{
  "name": "whiteboard-sketch-app",
  "version": "1.0.0",
  "description": "Browser-only whiteboard app using vanilla HTML5, CSS, and JavaScript",
  "type": "module",
  "scripts": {
    "test": "vitest --run"
  },
  "devDependencies": {
    "fast-check": "^3.15.0",
    "vitest": "^1.6.0",
    "@vitest/coverage-v8": "^1.6.0",
    "jsdom": "^24.0.0"
  },
  "vitest": {
    "environment": "jsdom"
  }
}
